# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

## Environment Properties

# The Minecraft version must agree with the Forge version to get a valid artifact
minecraft_version=26.1.2
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[26.1.2,26.2)

## Neo
# The neo version must agree with the Minecraft version to get a valid artifact
neo_version=26.1.2.99
# The neo version range can use any version of neo as bounds or match the loader version range
neo_version_range=[26.1.2,)
# The loader version range can only use the major version of Neo/FML as bounds
neo_loader_version_range=[3,)
# The loader version range can only use the major version of Neo/FML as bounds
neo_loader_version_range=[3,)
moddevgradle_version=2.0.142

# neo directly reads these properties
# the version the mappings were created for, does not need to match the current mc version
neogradle.subsystems.parchment.minecraftVersion=26.1.2
# the mapping version
neogradle.subsystems.parchment.mappingsVersion=2025.07.06

## Mod Properties
group=com.klikli_dev
mod_id=occultism_kubejs
mod_name=Occultism KubeJS
mod_license=MIT
mod_version=1.0.0
mod_authors=Kli Kli
mod_description=KubeJS Plugin for Occultism

## Dependency Properties
kubejs_version=26.1.2-8.0.4
kubejs_version_range=[26.1.2-8.0.4,)

occultism_version=1.248.1
occultism_version_range=[1.248.0,)

curios_version=15.0.0

jei_version=29.29.0.86
